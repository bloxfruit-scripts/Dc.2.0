# Discord Bot

## Features
- /send message:<text> count:<number>
- Sends messages anonymously multiple times

## Setup
1. Store your bot token as an environment variable named `TOKEN` (on Railway or locally in `.env`).
2. Install dependencies: `pip install -r requirements.txt`
3. Run the bot: `python main.py`
